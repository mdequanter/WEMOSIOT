You can use Pycharm to execute and change following code.

Create new project with Conda environment on python 3.8

pip install paho-mqtt

Launch the script pushMqtt.py
(It will connect to your localhost Mosquitto broker and publish an message every 5 seconds. Exactly as you would see when connecting to the real broker)